<nav class="navbar is-transparent">
    <div class="navbar-brand">
        <!-- Logo du menu -->
        <a class="navbar-item" href="https://bulma.io">
            <img src="{{ asset('assets/img/logo.png') }}" alt="Logo">
        </a>

        <!-- Burger du menu -->
        <div class="navbar-burger" data-target="menu">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </div>

    <div id="menu" class="navbar-menu">
        <!-- Partie située à gauche -->
        <div class="navbar-start">
            <a href="{{ path('app_accueil') }}" class="navbar-item">Accueil</a>
            <!-- Dropdown "Compétition" -->
            <div class="navbar-item has-dropdown is-hoverable">
                <a class="navbar-link" href="#">Compétition</a>
                <!-- Les éléments présents dans le dropdown "Compétition" -->
                <div class="navbar-dropdown is-boxed">
                    <a href="{{ path('app_infos_competition') }}" class="navbar-item">Infos compétition</a>
                    <a href="{{ path('app_classement') }}" class="navbar-item">Classement</a>
                    <a href="{{ path('app_planning') }}" class="navbar-item">Planning</a>
                </div>
            </div>
            <!-- Dropdown "Compétiteur" -->
            <div class="navbar-item has-dropdown is-hoverable">
                <a class="navbar-link" href="#">Compétiteur</a>
                <!-- Les éléments présents dans le dropdown "Compétiteur" -->
                <div class="navbar-dropdown is-boxed">
                    <a href="{{ path('app_competiteur_list') }}" class="navbar-item">Liste Compétiteur et notes</a>
                </div>
            </div>
            <a href="{{ path('app_contact') }}" class="navbar-item">Contact</a>
        </div>
    </div>


        <!-- Partie située à droite -->
        {% block isNotLogged %}
            <div class="navbar-end">
                <div class="navbar-item">
                    <div class="buttons">
                        <a class="button is-primary">
                            <strong>Inscription</strong>
                        </a>
                        <a class="button is-light">
                            Connexion
                        </a>
                    </div>
                </div>
            </div>
        {% endblock %}
    </div>
</nav>